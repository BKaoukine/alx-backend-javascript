export default function createEmployeesObject(departmentName, employees) {
  const worker = employees;
  function iteratorarra(worker) {
    let employee;
    employees.map(worker);
    return employee;
  }
  return {
    [departmentName]: [[iteratorarra]],
  };
}
